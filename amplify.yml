version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Use Node 20"
        - nvm install 20 || true
        - nvm use 20 || true
        - node -v
        - npm -v
        - npm install -g npm@latest
        - cd artifacts/mockup-sandbox
        - npm install --legacy-peer-deps --loglevel verbose
    build:
      commands:
        - cd artifacts/mockup-sandbox
        - npm run build
  artifacts:
    baseDirectory: artifacts/mockup-sandbox/dist
    files:
      - '**/*'
  cache:
    paths:
      - artifacts/mockup-sandbox/node_modules/**/*
