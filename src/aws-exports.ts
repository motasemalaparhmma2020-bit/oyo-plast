// Placeholder aws-exports configuration for Amplify.
// After you provision Auth and other backend resources via AWS Amplify Console,
// replace the contents below with the generated configuration (the file usually named aws-exports.js).

const awsExports = {
  // Example shape:
  // Auth: {
  //   region: 'us-east-1',
  //   userPoolId: 'us-east-1_XXXXXXXXX',
  //   userPoolWebClientId: 'xxxxxxxxxxxxxxxxxxxxxxxxxx',
  //   mandatorySignIn: true,
  // }
};

export default awsExports;
