import auth from "./auth/resource";

export default {
  version: 1,
  name: "amplify-backend",
  resources: {
    auth
  },
  // Note: This file provides a static, source-controlled configuration for Amplify Gen2.
  // It does NOT contain any credentials, environment variables, or provisioning logic.
};
