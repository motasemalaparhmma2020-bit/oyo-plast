## Packages
framer-motion | Smooth page transitions and micro-interactions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge Tailwind classes without conflicts

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'IBM Plex Sans Arabic'", "sans-serif"],
  display: ["'IBM Plex Sans Arabic'", "sans-serif"],
}
RTL layout direction enabled globally in index.css
Currency formatting needs to handle SAR (Saudi Riyal)
Static images will be used for categories
