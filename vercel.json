{
  "buildCommand": "cd artifacts/mockup-sandbox && npm install && npm run build",
  "outputDirectory": "artifacts/mockup-sandbox/dist",
  "installCommand": "npm install --registry=https://registry.npmjs.org"
}
